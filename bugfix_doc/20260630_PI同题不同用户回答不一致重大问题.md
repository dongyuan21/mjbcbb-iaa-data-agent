# 2026-06-30 PI 同题不同用户回答不一致重大问题

> 级别：重大问题  
> 状态：`fixed_and_live_replay_validated`（D30 route-bound verified SQL 已部署；最终全量 N=3 中对应 case 为 3/3 PASS）

## 当前验收结论（2026-07-15）

行为提交 `54df4d8f` 的最终全量 N=3 中，`PI_D30_ARPU_TOP20_CHANNEL_COUNTRY_CONCENTRATION` 为 3/3 PASS，3 条 query records 全部成功，无 failed check、blocked 或 flaky。下文 2026-06-30 的 test FAIL 是修复前历史证据，原样保留。

该 machine-auth N=3 证明 route-bound 运行链稳定，不等同于“不同 SSO 用户的页面体验完全一致”；如需证明跨用户 UI 一致性，仍须独立人工验收并记录证据。

## PI Replay 回归（2026-06-30）

| 批次 | 命令 | 结果 |
|---|---|---|
| 全量 5 cases | `pi_replay_regression.py --timeout 420` | **中断**：第 2 个 case 触发 qwen3.7-max `inappropriate-content` 审核，脚本异常退出 |
| 仅 D30 case | `--cases .scratch/pi_replay_d30_only.yaml` | **FAIL** · session `e335d8f4-e74d-4f82-8923-f0c57ebcbfc6` |

D30 单 case 与 **002494 第 1 次完全一致**：

- 7 次工具，engine 均为 `unknown`（读文件/grep），**0 次 MC 业务 SQL**
- 熔断：`no_progress_tools`
- 回答：「查询未完成 · 核心业务 SQL 尚未执行」

报告路径：`eval/PI调用日志复核_20260630/20260630_PI自动回归测试报告.md`

**解读**：本地 verified SQL / `text2sql` guardrail **尚未部署到 test runtime**，PI 仍自由探索 → replay 继续不稳定。下一步需 **部署 runtime + 知识召回 verified SQL** 后再跑回归。  
> 环境：test · Shanhai env_id=811 · MySQL trace 复核日 2026-06-30

## 问题概览

| 项 | 内容 |
|---|---|
| 编号 | MAJOR-20260630-PI-SAME-Q-DIFF-USER |
| 用户报告 | 同一问题「分析 kcolb_tsalb_gp 每个渠道每个国家，top20% 累积 d30arpu，能占 d30 总 arpu 的比例」，工号 **002452** 能拿到完整分析，**002494** 两次被阻断 |
| 初步怀疑 | 用户歧视 / 权限差异 |
| 复核结论 | **非用户歧视**；同一 runtime、同一模型、同一 guardrail；差异来自 **PI Agent 非确定性探索路径 + 相同熔断规则** |

## 同一问题、不同用户：Trace 对比（核心证据）

问题 fingerprint 相同：`550023a8b875e05dde52c109260b8506449761e672ca9d89319b8b70de1f109c`

| 维度 | 002452（19:39） | 002494 第1次（19:26） | 002494 第2次（19:30） |
|---|---|---|---|
| session | `73cb4c81-…` | `f324a265-…` | `b2066b04-…` |
| 耗时 | **76s** | **~1ms** | **1.4s** |
| 工具调用 | 13 次（10 成功） | 7 次（全为读文件/grep，**0 次业务 SQL**） | 5 次（4 失败） |
| 熔断 | 未触发 | `no_progress_tools` | `consecutive_tool_failures` |
| 用户可见 | 完整报告（历史口径：组合 Top20% + 部分 SDK 路径） | 「查询未完成 · 核心 SQL 未执行」 | 「查询未完成 · 连续工具失败」 |

### 为什么 002452「看起来能跑通」

- Agent 探索更深（13 轮），中间有 **成功 MC 查询打断连续失败链**（80 行 / 20 行结果）。
- 虽有 `SELECT *` / execution_error，但未达到 **连续 3 次失败** 熔断线。
- **注意**：002452 报告走的是 **解释 B（渠道×国家组合 Top20%）**，与业务最终确认的解释 A 不一致。

### 为什么 002494「被阻断」

**第 1 次**：7 轮工具全是知识检索类（`unknown` engine），从未执行 MC/CK → 连续 6 次无业务证据 → `no_progress_tools`。

**第 2 次**：选错表 `ads_market_device_camp_dau_behavior_di`（DA 活跃聚合，非用户级 D30 回收）→ 触发 `unbounded_select_star`、`pii_blocked`（输出 `distinct_id`）、execution_error → 连续 3 失败 → `consecutive_tool_failures`。

### 根因归类

```text
同一 fingerprint 问题
  ├─ 非权限：无按工号 ACL / 白名单 / guardrail 差异
  ├─ 非确定性：LLM 选表、探活顺序、是否先读文件 → 路径分叉
  └─ 确定性熔断：pi_agent._update_tool_progress_state()
        · consecutive_no_progress >= 6 → no_progress_tools
        · consecutive_failures >= 3   → consecutive_tool_failures
```

相关代码：`runtime/backend/app/services/pi_agent.py`（约 947–960 行）。

## 业务口径澄清（2026-06-30 已确认）

| 项 | 定稿 |
|---|---|
| Top20% | **A**：每个 `(media_source, country)` **格子内**用户 Top20% |
| 收入 | **DA 行为** `ad_revenue` |
| Cohort | 仅 **D30 完整**安装日；`t-1` 数据日时成熟窗 `install_date ∈ [t-59, t-30]` |

002452 历史 75.91% **不可**作为本口径基准（解释 B + SDK）。

## 修复与治理（已落地）

### 1. Verified SQL（消除「手气查数」）

| 资产 | 路径 |
|---|---|
| Verified SQL | `da_assets/verified_sql/vsql_20260630_d30_top20_user_concentration_channel_country.md` |
| Runtime canonical SQL | 上述 verified 文档 `### Canonical SQL` 下唯一的 `sql` fence |
| 索引 | `da_assets/index.yaml` · `vsql_20260630_d30_top20_user_concentration_channel_country` |

**2026-06-30 MaxCompute 在线验证（实时探测，非旧快照）**：

- 表 latest `dt = 2026-06-30`；行为上界 `t-1 = 2026-06-29`
- `googleadwords_int × US` 成熟 30 日窗：cohort=555,408，**Top20% 用户占该格 D30 DA 收入 89.84%**
- SQL 修复：MaxCompute 禁止 `CROSS JOIN params` → 行为表过滤改用 `(SELECT … FROM params LIMIT 1)`

### 2. 路由 guardrail

- `task_routes/text2sql_or_sql_planning.yaml`：top20%/集中度问题强制解释 A + DA 表路由 + 禁止错表/PII 外显

### 3. PI 自动回归

- 用例：`eval/pi_replay_regression/cases.yaml` · `PI_D30_ARPU_TOP20_CHANNEL_COUNTRY_CONCENTRATION`
- semantic check：`d30_top20_user_concentration_within_segment`（`tools/scripts/pi_replay_regression.py`）

### 4. 详细工程项文档

- `bugfix_doc/20260630_PI同题稳定性_D30渠道国家Top20集中度.md`

## 后续独立验收 / 治理

| 项 | 状态 |
|---|---|
| PI replay 全量/本 case 回归 | **已完成**；`54df4d8f` 最终全量 N=3 中 D30 为 3/3 PASS。旧 FAIL 保留为历史基线 |
| test runtime 部署 verified SQL + 路由 guardrail | **已完成并 live 验证** |
| 不同 SSO 用户的页面体验一致性 | `evidence_gap`；不能由 machine-auth N=3 代替 |
| 熔断策略微调（text2sql 先执行 core SQL 再 no_progress） | P1 排期 |
| review-day 同 fingerprint 跨 user 不一致告警 | P2 排期 |

## 给业务 / 运营的说明

- **不是歧视用户**；同一问题在不同会话可能因 Agent「手气」得到完全不同体验。
- 稳定做法：Agent 默认召回 **verified SQL**，而非每次自由探索。
- 本问题正确口径为 **每渠道×国家格子内用户 Top20%**，不是 002452 报告里的「高 ARPU 组合占全站比例」。

## 关联

- 同类：`bugfix_doc/20260623_PI路由误判与空结果掩盖重大问题.md`
- 复核工具：`tools/scripts/pi_review_tool.py review-day --date 2026-06-30 --user-id 002452|002494`
