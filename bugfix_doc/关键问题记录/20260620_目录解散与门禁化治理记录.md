# 20260620 目录解散与门禁化治理记录

> 记录日期：2026-06-20
> 范围：解散 `case_studies/`、`examples/` 两个游离顶层目录；统一三库 manifest 命名；把"靠人盯"的边界改成脚本门禁。
> 触发：用户在三层对齐评审后拍板——`case_studies`/`examples/audience` 文件夹不应独立存在、内容融合进工程；manifest 命名要统一；三层定义要单源化；`ai_ck/metrics` 与 `semantic_contract/metrics` 边界不接受靠人盯。
> 前序：`20260620_知识层收敛与语义契约归并记录.md`（三层对齐 + 语义契约并入 knowledge）。

## 一、目录解散（文件夹消失，内容并入工程）

`case_studies/`、`examples/` 不是孤儿：解散前它们被 25+ 张表卡当 `source_docs` / `evidence`。所以是"解散文件夹 + 内容并入对应知识库审计层 + 重指向全部引用"，不是裸删。

| 旧路径 | 新位置 | 理由 |
|---|---|---|
| `case_studies/wide_hi/` | `ai_hive/audit_archive/evidence/wide_hi/` | MC 侧血缘审计证据 |
| `case_studies/mi_roi360_board_recon/` | `ai_ck/audit_archive/mi_roi360_board_recon/` | CK/MI 反推对账证据 |
| `examples/audience/` | `da_assets/candidate_sql/audience/` | 未验证 SQL 样例与 candidate_sql 概念重合，统一治理 |

执行方式：`git mv` 保留历史 + 确定性迁移脚本重写引用（segment 映射 + 表卡 source_docs 根相对归一）。迁移顺带修复了历史遗留的 61 个 `source_docs_unresolved`（`../../` 相对层级差一级，长期解析不了），表卡 quality_gaps 从 66 降到 5（仅剩等人确认的 known_pitfalls）。

## 二、manifest 命名统一

三库顶层 `agent_manifest.yaml` 结构本已一致；不一致的是召回注册表名。

| 库 | 旧注册表 | 新注册表 |
|---|---|---|
| ai_hive / ai_ck | `agent_knowledge/catalog.yaml` | 不变 |
| knowledge | `agent_knowledge/manifest.yaml` | `agent_knowledge/catalog.yaml` |

统一后规则极简：**每库都是 `agent_manifest.yaml`(挂载) + `agent_knowledge/catalog.yaml`(召回注册表)**，并消除了 knowledge 里 `manifest.yaml` 与 `agent_manifest.yaml` 近似名并存。`run_regression.py`、两个门禁脚本、MAP、AGENTS、README、runbook 等 22 处引用同步更新。

## 三、三层定义单源化

原三层"用途分层规范"在 `ai_hive`/`ai_ck`/`knowledge` 三个 README 各写一份（事实重复，易漂移）。现抽到唯一 canonical：

- `data_agent_plan/知识库三层结构规范.md`（三层模型 + manifest 约定 + 跨库归属 + 门禁边界 + 历史归并记录）。
- 三个 README 的通用三层表改为一行指向规范，只保留本库具体文件清单。

## 四、边界门禁化（不靠人盯）

| 边界 | 门禁实现 |
|---|---|
| 顶层目录必须被路由图分类（canonical_owner / never_default_recall / reference_only_dirs） | `check_agent_retrieval_map.py` 新增 `check_top_level_coverage` |
| `showoff/`、`关键问题记录/`、`术语解释/` 等保留的人读目录纳入分类 | `AGENT_RETRIEVAL_MAP.yaml` 新增 `reference_only_dirs` |
| 表卡 `source_docs` 仓内路径必须解析 | `check_table_card_quality.py`：仓内顶层路径未解析 → 硬错误（G1）；外部本地源 obsidian/raw 仍为软缺口 |
| 表卡正文不得含跨表指标/语义 | `check_table_card_quality.py`：禁 `requires_join`、顶层 `metrics:`、`type: cross_table`（F1） |
| `ai_ck/metrics` 与 `semantic_contract/metrics` 边界 | 新增 `check_metric_layer_boundary.py`：metrics.yaml 声明 canonical owner；每个 ai_ck/metrics 文档必须声明层级边界并指向 owner；ai_ck/metrics 禁出现跨表定义键（E1） |
| 弃用顶层目录不得回潮 | 两个门禁的 deprecated_route_paths 新增 `case_studies/`、`examples/`、`agent_knowledge/manifest.yaml` |

`check_metric_layer_boundary.py` 已接入 `run_regression.py`，成为标准回归套件一员。

## 五、验收

本次全部脚本门禁实跑通过（静态门禁，未重连 MC/CK，freshness 走快照）：

| 检查 | 结果 |
|---|---|
| semantic build | PASS，validation passed |
| semantic composer 回归 | 16/16 PASS |
| knowledge consistency | 0 hard / 0 warnings |
| agent retrieval map（含顶层覆盖门禁） | 0 hard / 0 warnings |
| table card quality（含 F1/G1） | 0 hard / 0 warnings，quality_gaps 66→5 |
| metric layer boundary（E1） | 0 errors，5 ck 文档已声明边界 |
| sql promotion / partition guardrails | 0 errors |
| agent regression | PASS，cases 10/10、knowledge 7/7、routes 6/6、metric_layer_boundary=PASS |

## 六、遗留观察项

1. `da_assets/candidate_sql/audience/*.sql` 是未验证样例，未登记 index.yaml（gate 不扫 `.sql`）；如需晋升走 `SQL晋升治理.md`。
2. obsidian vault 引用（`../../obsidain/...`）解析依赖本机父级 vault 存在；换机会变不可达，但首段非仓内目录，G1 按外部源豁免，不阻断。
3. 三层规范是单源；后续新增知识库或新层必须先改 `data_agent_plan/知识库三层结构规范.md`，再落地，避免又回到三处复制。
