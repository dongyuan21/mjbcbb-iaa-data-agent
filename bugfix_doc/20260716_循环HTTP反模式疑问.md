循环 HTTP 反模式：PI 通过 bash + curl 调用 FastAPI 自身的 /api/pi-tools/* 端点获取数据。每次查询经历 用户 → FastAPI → PI subprocess → curl FastAPI → MC subprocess → 结果回到 PI → 回到 FastAPI → 回到用户，至少 4 层进程 + 1 次 HTTP 自调用。正确做法是 PI 直接调用数据工具脚本（或通过 MCP），不需要绕回 FastAPI。

什么是反模式？

 