# 数仓 Data Agent 项目面试准备材料

> 用途：面试前快速复习。文中“当前工程事实”来自仓库材料；带“需按实际补充”的内容不要直接照搬。
> Runtime 口径以 `showoff/00-系统全景/当前系统架构图.md` 为准（单一 Python Runtime + `MinimalToolLoop`）；勿再按历史 PI RPC 表述。

## 一、30 秒项目介绍

我参与建设的是一套面向投放和 ROI 分析的 Data Agent。它不是简单把自然语言翻译成 SQL，而是把表卡、业务口径、语义层、已验证 SQL、分析 SOP、数据新鲜度和回归门禁组织成一条可追溯的分析链路。用户提出 ROI、DNU、素材、Campaign 或实验问题后，系统先做任务路由和知识检索，再选择 MaxCompute/Hive 或 ClickHouse 数据源，执行只读查询，最后输出带数据来源、置信度和风险说明的分析结果。

## 二、2 分钟展开版

### 背景问题

投放分析涉及 MaxCompute、ClickHouse、MI/ROI360、AF、埋点和实验数据，表多、粒度不同、指标口径容易混淆。单纯依赖 LLM 会出现选错表、误用分母、把旧数据当新数据、把截图或草稿当事实等问题。

### 我的解决思路

先建设数据知识基座，再接 Runtime：

1. 用表卡描述表的粒度、分区、字段、join key、PII 和查询陷阱。
2. 用语义契约统一 ROI、LTV、DNU、留存等跨表指标和关联关系。
3. 把 SQL 分为 `candidate` 和 `verified`，新 SQL 先有字段证据，再做 schema、dry-run 和小窗口验证。
4. 用 `AGENT_RETRIEVAL_MAP.yaml` 和 `task_routes/` 做按任务加载，避免整仓塞进上下文。
5. 用 freshness、知识一致性、表卡质量和 Agent regression 做门禁。
6. 用 FastAPI + React + **进程内 `MinimalToolLoop`** 落地只读 Web Runtime：`runtime_worker` + 持久化 Job、MC/CK 只读 executor、MySQL 权威事件与 SSE 向前端推送过程和结果。（历史曾用外部 PI harness，已退役。）

### 当前成果

仓库快照记录：ai_hive 104 张表、ai_ck 20 张表；语义模型 11 个实体、38 个维度、50 个指标；已验证 SQL 22 条；问答回归 8/8，通过 0 freshness-blocked。Runtime 侧：test lane 已验证的只读链路——FastAPI、React、`runtime_worker`、MySQL Job/事件持久化、Redis 队列、**固定 9 工具**的 `MinimalToolLoop` 与 MC/CK 只读执行器。

> 以上是仓库快照，不等同于线上 SLA、业务收益或全量准确率。若面试官追问实际收益，补充你真实测量过的耗时、覆盖率、人工节省或错误率。

## 三、架构怎么讲

```text
用户问题
  -> POST /api/jobs：鉴权，完整 request_payload + submitted event + outbox 同事务写 MySQL
  -> Redis 队列唤醒 -> runtime_worker 租约 claim Job
  -> 任务路由：ROI / DNU / 素材 / 实验 / SQL 审查（route 为软先验）
  -> 按需装配 policy、语义契约、表卡、verified SQL（控制面，非全库 RAG）
  -> 进程内 MinimalToolLoop（固定 9 个受治理工具；不开放任意文件系统 / 投放写 API）
  -> 需要 SQL 时：先 read_data_contract_view -> 服务端 SQL L5 binding
  -> runtime/tools/data_query.py -> MC/CK 只读 executor
  -> 同事务提交 answer、trace、query_records、job 终态与 durable SSE events -> 前端
  -> 复盘后沉淀 SQL、SOP 或 case（人工晋升，非 Agent 直写 verified）
```

核心设计是「知识层负责约束，Runtime 拥有状态与终态，L5 负责 SQL 能否执行，数据执行层负责取数，门禁负责阻止不可信结论」。MC 和 CK 不直接跨库 join，而是在应用层对齐。Redis 只做队列与唤醒；**最终答案与终态以 MySQL 事务为准**。

权威架构图：`showoff/00-系统全景/当前系统架构图.md`。

## 四、最值得讲的三个难点

### 难点 1：Text2SQL 的可靠性

难点不在生成 SQL，而在业务问题到表、字段、粒度和口径的映射。解决方案是任务路由 + 语义契约 + 表卡 + 字段证据 + candidate/verified 分层。没有足够证据时，系统应输出缺口或待验证项，而不是编造结果。Runtime 侧 SQL 须经 `DataContractView` 与服务端 binding，模型自报表名或「已取证」不构成执行授权。

### 难点 2：上下文太大

表卡全量约 302K 字符，不能每轮全部加载。采用控制面与重索引分离、任务一文件、命中才读单张表卡、verified SQL top-K 和字符上限，Runtime 单次 prompt 控制在约 3–8K 的实测范围。当前仍不是 embedding 或完整 token budget 系统，不能夸大为「已经解决上下文压缩」。`Context Compiler` 等能力在演进中，是否已接入 provider 主链以当前源码与部署为准。

### 难点 3：数据延迟与错误结论

空结果不一定代表业务变差，可能是分区未到或源表延迟。因此在分析前做 MC/CK 连通性、`SELECT 1` 和目标表分区 probe，并刷新 freshness snapshot；输出时区分实时探测、旧快照和未验证状态。投放动作保持只读，停投、放量和预算调整交给业务 owner。

## 五、个人贡献表达模板（按实际改）

我主要负责【知识库治理 / Text2SQL 路由 / Runtime 后端 / MC-CK 执行器 / 回归门禁】。具体做了三件事：第一，把【某类业务】的表、口径和 SQL 整理成机器可读资产；第二，实现【某模块】并打通【输入—检索—执行—输出】链路；第三，用【某个检查或回归集】验证错误边界。结果是【真实指标或交付结果】；目前仍有【真实未完成项】，所以系统定位是只读分析 POC/阶段性能力，而不是自动投放决策系统。

## 六、常见追问与回答

**Q：和普通 RAG + Text2SQL 有什么不同？**  
A：重点不是把文档塞进上下文，而是把表卡、语义、SQL 资产、数据状态和验证状态串成可执行契约；输出还要带证据和边界。RAG 只是检索手段，不是可靠性方案本身。

**Q：为什么不让模型直接写 SQL？**  
A：直接生成会掩盖表粒度、分母、分区和字段语义错误。我们允许生成 candidate SQL，但必须经过证据、schema、dry-run、小窗口和晋升门禁，verified SQL 才能进入稳定召回。线上执行还要过 `DataContractView` 与服务端 SQL binding（只读、PII、分区、表绑定）。

**Q：为什么不用 LangGraph / DeerFlow，而是自研 MinimalToolLoop？**  
A：早期用过外部 harness（含 PI），后来为 **控制权收拢** 迁到进程内 Python 主链。投放只读场景更需要：固定工具白名单（9 个受治理工具）、SQL 授权与终态判定留在 Runtime/L5、去掉 Node 子进程与 bash 跳板、Job + Worker 可恢复。LangGraph/DeerFlow 适合通用长程编排与沙箱；我们 L4 刻意保持极简 loop，不把终态权交给外部框架。核心资产是数据底座与控制面，不是 loop 框架选型。

**Q：为什么从 PI 迁到自研 loop？**  
A：PI 能快速起 agent loop，但 spawn 子进程、bash 调 `data_query.py`、事件映射让 **SQL 授权、工具面收窄、Job 持久化** 难焊在 Python 侧。迁到 `MinimalToolLoop` 后：单一 Python Runtime、无外部 Node 执行器、不开放任意文件系统访问；模型只能提议 tool call，能否执行由服务端裁决。

**Q：Runtime 的一次请求怎么走？**  
A：`POST /api/jobs` 写入完整 `request_payload` 与 outbox（MySQL 同事务）→ Redis 唤醒 → `runtime_worker` 租约领取 → `KnowledgeRouter` 按 route 装配受控先验 → `MinimalToolLoop` 在固定 9 工具内多轮取证 → 需要查数时先 `read_data_contract_view`，再经 L5 binding 走 `data_query.py` 与 MC/CK 只读 executor → 答案、trace、query_records、job 终态与 SSE 事件 **同事务** 提交；前端通过 durable events 续传。

**Q：toolcall 怎么建设的？有写文件能力吗？**  
A：不是开放 bash/file 工具，而是 `RuntimeToolRegistry` 注册 **固定 9 个受治理工具**；循环只在 `MinimalToolLoop` 的模型 ↔ 工具交换内发生，有步数/预算上限。不开放 SQL 写入、投放平台写 API、任意文件系统访问。持久化写 MySQL（job、trace、query_records），不是让 Agent 改知识库或写本地文件。

**Q：当前系统有什么不足？**  
A：结构化多轮状态（`TurnResolution` / checkpoint）尚未在所有 API/SSE/Worker 入口完全原子闭环；`Context Compiler` 未必已是 provider 主链输入；探索型取证默认 `off`；E2E 与部署同 commit 验收仍需加强；表卡/场景覆盖率非全量。下一步：多轮状态闭环、上下文编译接入主链、更细 trace/token 观测、报告存储与权限生产化。

**Q：如何证明回答可信？**  
A：不是只看答案像不像，而是检查数据源、时间窗口、SQL、字段证据、freshness、知识状态、`DataContractView` 读取记录、binding 决策与回归结果。对未验证内容明确标为 candidate、draft 或 connectivity_unverified。

## 七、面试前必须补齐的个人信息

- 你在项目中的明确职责和代码/文档范围
- 一个你亲自解决的具体问题，以及前后对比（例如 PI 退役 / MinimalToolLoop 迁移、Job 持久化、SQL binding）
- 一个真实业务案例：问题、分析、结论、后续动作
- 实际使用过的 Python/SQL/FastAPI/React 细节（TypeScript 若未参与 Runtime 主链勿夸大）
- 可公开的耗时、覆盖率、准确率、节省人力或交付周期
- 为什么做这个项目、遇到的最大失败，以及如何修正

## 八、面试表达底线

把「已完成」「已验证」「规划中」分开；不要把 8/8 回归说成线上准确率，也不要把表卡覆盖说成业务全覆盖。不要展示凭证、用户级明细、设备 ID、IP 或内部 token。涉及投放动作时强调系统当前是只读分析和风险提示，最终决策由业务 owner 拍板。Runtime 表述统一为 **单一 Python Runtime + MinimalToolLoop**，不要说「还在用 PI」除非明确指历史阶段。
