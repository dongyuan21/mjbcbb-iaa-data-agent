# 数仓 Data Agent 项目面试准备材料

> 用途：面试前快速复习。文中“当前工程事实”来自仓库材料；带“需按实际补充”的内容不要直接照搬。

## 一、30 秒项目介绍

我参与建设的是一套面向投放和 ROI 分析的 Data Agent。它不是简单把自然语言翻译成 SQL，而是把表卡、业务口径、语义层、已验证 SQL、分析 SOP、数据新鲜度和回归门禁组织成一条可追溯的分析链路。用户提出 ROI、DNU、素材、Campaign 或实验问题后，系统先做任务路由和知识检索，再选择 MaxCompute/Hive 或 ClickHouse 数据源，执行只读查询，最后输出带数据来源、置信度和风险说明的分析结果。

## 二、2 分钟展开版

### 背景问题

投放分析涉及 MaxCompute、ClickHouse、MI/ROI360、AF、埋点和实验数据，表多、粒度不同、指标口径容易混淆。单纯依赖 LLM 会出现选错表、误用分母、把旧数据当新数据、把截图或草稿当事实等问题。

### 我的解决思路

先建设数据知识基座，再接 Runtime：

1. 用表卡描述表的粒度、分区、字段、join key、PII 和查询陷阱。
2. 用语义契约统一 ROI、LTV、DNU、留存等跨表指标和关联关系。
3. 把 SQL 分为 `candidate` 和 `verified`，新 SQL 先有字段证据，再做 schema、dry-run 和小窗口验证。
4. 用 `AGENT_RETRIEVAL_MAP.yaml` 和 `task_routes/` 做按任务加载，避免整仓塞进上下文。
5. 用 freshness、知识一致性、表卡质量和 Agent regression 做门禁。
6. 用 FastAPI + React + PI RPC 落地只读 Web Runtime，MC/CK 通过独立 executor 查询，SSE 向前端推送过程和结果。

### 当前成果

仓库快照记录：ai_hive 104 张表、ai_ck 20 张表；语义模型 11 个实体、38 个维度、50 个指标；已验证 SQL 22 条；问答回归 8/8，通过 0 freshness-blocked。另有 Runtime Phase 0 脚手架：FastAPI、React、PI RPC、MySQL 持久化和 MC/CK 只读执行器。

> 以上是仓库快照，不等同于线上 SLA、业务收益或全量准确率。若面试官追问实际收益，补充你真实测量过的耗时、覆盖率、人工节省或错误率。

## 三、架构怎么讲

```text
用户问题
  -> 任务路由：ROI / DNU / 素材 / 实验 / SQL 审查
  -> 按需加载 policy、语义契约、表卡、verified SQL
  -> 形成字段证据和 candidate SQL
  -> schema / dry-run / 小窗口 / freshness 门禁
  -> PI agent loop 调用只读 data_query 工具
  -> MC 或 CK 执行
  -> FastAPI 通过 SSE 返回过程、SQL evidence、结果和报告
  -> 复盘后沉淀 SQL、SOP 或 case
```

核心设计是“知识层负责约束，Runtime 负责编排，数据执行层负责取数，门禁负责阻止不可信结论”。MC 和 CK 不直接跨库 join，而是在应用层对齐，减少跨引擎语义和权限问题。

## 四、最值得讲的三个难点

### 难点 1：Text2SQL 的可靠性

难点不在生成 SQL，而在业务问题到表、字段、粒度和口径的映射。解决方案是任务路由 + 语义契约 + 表卡 + 字段证据 + candidate/verified 分层。没有足够证据时，系统应输出缺口或待验证项，而不是编造结果。

### 难点 2：上下文太大

表卡全量约 302K 字符，不能每轮全部加载。采用控制面与重索引分离、任务一文件、命中才读单张表卡、verified SQL top-K 和字符上限，Runtime 单次 prompt 控制在约 3–8K 的实测范围。当前仍不是 embedding 或完整 token budget 系统，不能夸大为“已经解决上下文压缩”。

### 难点 3：数据延迟与错误结论

空结果不一定代表业务变差，可能是分区未到或源表延迟。因此在分析前做 MC/CK 连通性、`SELECT 1` 和目标表分区 probe，并刷新 freshness snapshot；输出时区分实时探测、旧快照和未验证状态。投放动作保持只读，停投、放量和预算调整交给业务 owner。

## 五、个人贡献表达模板（按实际改）

我主要负责【知识库治理 / Text2SQL 路由 / Runtime 后端 / MC-CK 执行器 / 回归门禁】。具体做了三件事：第一，把【某类业务】的表、口径和 SQL 整理成机器可读资产；第二，实现【某模块】并打通【输入—检索—执行—输出】链路；第三，用【某个检查或回归集】验证错误边界。结果是【真实指标或交付结果】；目前仍有【真实未完成项】，所以系统定位是只读分析 POC/阶段性能力，而不是自动投放决策系统。

## 六、常见追问与回答

**Q：和普通 RAG + Text2SQL 有什么不同？**  
A：重点不是把文档塞进上下文，而是把表卡、语义、SQL 资产、数据状态和验证状态串成可执行契约；输出还要带证据和边界。RAG 只是检索手段，不是可靠性方案本身。

**Q：为什么不让模型直接写 SQL？**  
A：直接生成会掩盖表粒度、分母、分区和字段语义错误。我们允许生成 candidate SQL，但必须经过证据、schema、dry-run、小窗口和晋升门禁，verified SQL 才能进入稳定召回。

**Q：为什么选择 PI，而不是 LangGraph？**  
A：当前阶段需要快速复用成熟的 agent loop、RPC 和工具事件流；FastAPI 保留 Web API、SSE、知识注入、权限和持久化。LangGraph 更适合后续复杂状态机，但当时会增加编排成本。

**Q：Runtime 的一次请求怎么走？**  
A：FastAPI 接收问题，KnowledgeRouter 注入相关知识，然后 spawn PI RPC 进程；PI 通过 bash 调用独立的 `data_query.py`，由 MC/CK executor 执行只读查询，PI 事件被映射为 SSE 返回前端，查询记录、trace 和报告元数据写 MySQL。

**Q：当前系统有什么不足？**  
A：Phase 0 每次请求 spawn PI，没有跨轮 session；E2E 评测仍需加强；SSO、OSS、钉钉部分是 mock/no-op；覆盖率也不是全量。下一步是长驻进程、完整 E2E、权限和报告存储生产化，以及更细的 token/trace 观测。

**Q：如何证明回答可信？**  
A：不是只看答案像不像，而是检查数据源、时间窗口、SQL、字段证据、freshness、知识状态和回归结果。对未验证内容明确标为 candidate、draft 或 connectivity_unverified。

## 七、面试前必须补齐的个人信息

- 你在项目中的明确职责和代码/文档范围
- 一个你亲自解决的具体问题，以及前后对比
- 一个真实业务案例：问题、分析、结论、后续动作
- 实际使用过的 Python/TypeScript/SQL/FastAPI/React 细节
- 可公开的耗时、覆盖率、准确率、节省人力或交付周期
- 为什么做这个项目、遇到的最大失败，以及如何修正

## 八、面试表达底线

把“已完成”“已验证”“规划中”分开；不要把 8/8 回归说成线上准确率，也不要把表卡覆盖说成业务全覆盖。不要展示凭证、用户级明细、设备 ID、IP 或内部 token。涉及投放动作时强调系统当前是只读分析和风险提示，最终决策由业务 owner 拍板。
