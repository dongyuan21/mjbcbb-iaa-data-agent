# ua_phase3_v2_data

UA 决策行为沉淀 **Phase3 v2** 的样本与结果数据（双源、DiD 等产物）。

**平面归属**：离线证据层。方案/台账在 [`../UA决策行为沉淀/`](../UA决策行为沉淀/README.md)；总入口见 [`../离线/`](../离线/README.md)。

## 规则

1. **不是** Agent 默认召回事实源；只作专题复盘 / 训练 / 评估证据。
2. v1 路径 `ua_phase3_run/` 已废弃；文档若仍写 v1，以本目录为准。
3. 大 JSON/CSV 可能仅本地保留；以目录内实际文件与 UA 台账为准。
4. 与 `schema_probe_snapshots/`、`snapshots/` 同属离线证据归档族，由离线做导航收纳。
