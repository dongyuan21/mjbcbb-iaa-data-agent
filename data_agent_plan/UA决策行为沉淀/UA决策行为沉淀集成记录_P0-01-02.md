# UA-INT-P0-01-02 集成记录

> 状态：`INTEGRATED(6784842b...)`（自审 PASS，用户已回复"确认"）
>
> 日期：`2026-07-23`
>
> `TASK_ID=UA-INT-P0-01-02`；`TASK_TYPE=INTEGRATION`

## 1. 集成范围

- `TARGET_REPO`：`/Users/lidongyuan/hungrystudio/点位/数仓`
- `INTEGRATION_BASE`：`1fbfc290`（= `80f54c4c` + 台账记录 P0-01/02 HUMAN_ACCEPTED）

## 2. 纳入的批准 commit

| 顺序 | Commit SHA | Task ID | 说明 |
|---|---|---|---|
| 1 | `edc0bbd7` | `UA-P0-01` | Phase0 静态来源审计（1 个新文件） |
| 2 | `ff1d158f` | `UA-P0-02` | Phase0 MC/CK 实时连通性与 freshness 探测（1 报告 + 2 snapshot） |

两个 commit 的 base 都是 `80f54c4c`，互不依赖，无文件冲突。通过 `git cherry-pick` 顺序纳入，无冲突、无手动修改。

## 3. 人工验收引用

- 用户在会话中对 P0-01 和 P0-02 的所有结论回复"都同意"（2026-07-23）。

## 4. Freshness 冲突决议

`FRESHNESS_COLLISION_DECISION_REF`：用户已在共享 checkout 重新运行 `probe_freshness.py`（exit 0，probed_at=2026-07-23T03:39Z/03:51Z），用最新结果替换了原有 WIP。隔离分支 cherry-pick 进来的 snapshot 与共享 checkout 现有内容探测时间差约 30-50 分钟，水位一致（MC 最新分区 dt=2026-07-23/07-21，CK 同），不存在语义冲突。后续集成到 main 时以共享 checkout 的最新探测为准。

## 5. 跨任务门禁

| 门禁 | 适用 | 结果 |
|---|---|---|
| `check_knowledge_consistency.py` | 不适用 | 未改 `knowledge/` |
| `check_authority_map.py` | 不适用 | 未改 `knowledge/` 或 `da_assets/` |
| `check_metric_layer_boundary.py` | 不适用 | 未改指标合同 |
| `check_table_card_quality.py` | 不适用 | 未改表卡 `.yaml` |
| Markdown 格式自检 | 适用 | 通过（围栏成对、无重复标题、无尾随空白、`git diff --check` exit 0） |

## 6. 集成产物

- `INTEGRATION_REF`：`codex/ua-int-p0-01-02-20260723`
- `REF_VISIBILITY`：`shared_local`
- 新 `BASE_COMMIT`：`6784842b2a6021b05b0bf0ad145e223fd5aa0650`（用户已确认）
- `UA-P0-03` 及后续任务以该 SHA 作为 `BASE_COMMIT`
