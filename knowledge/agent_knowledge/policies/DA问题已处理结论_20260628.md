# DA 问题清单 - 已处理结论（2026-06-28）

## 状态

- 来源：原 `TODO/下周给DA的问题.md` 的“已处理结论（不再列入 DA 待问）”区块（该清单已于 2026-07-14 归档，已处理结论保留在本文件）
- 生效状态：`confirmed`
- 更新范围：MI ROI360 日常复盘口径、SDK / AF 回收口径、异常判断边界、媒体字段映射等

## 已确认条款

### DA-01 ROI7 / ROI30 复盘口径

- 日常复盘优先看 MI ROI360。
- 默认参数：
  - `revenue_source=sdk`
  - `forecast_version=v6`
  - `cost=total_cost_zhe`
  - `ROI7 = roi_6`
  - `ROI30 = roi_29`
- 对象默认按 MI 下钻维度理解，例如 `media_source`、`country`、`campaign_name`、`adset_name`、`ad_name`。
- 后续策略：移出 DA 待问清单；仅在需要 CK/MC 对账或明细溯源时再补对应 SQL。

### DA-02 SDK 回收与 AF 回收差异

- 日常统一口径优先 MI ROI360 与 SDK 回收。
- AF 回收作为对照口径，不作为 DA 标准拆解主线。
- 后续策略：业务若要求再对账，再单独沉淀 SDK/AF 的 verified SQL。

### DA-03 DNU 下滑来源

- 默认使用 MI 分析（如展示、点击、CTR、CVR、IPM、CPI、注册和消耗）。
- 默认不再让 DA 另行承担来源拆解。
- 后续策略：预装、organic 等特殊口径仍保留争议时单独建问题。

### DA-04 消耗没降但注册下降

- 优先看 MI 前端漏斗指标（展示、点击、CTR、CVR、IPM、CPI、注册和消耗）判断。

### DA-07 首日 ARPU 下滑

- 首日 ARPU 在第一天不应直接下强结论；默认关注第三天 ARPU 稳定性。

### DA-11 ROI 预测偏差

- 该问题默认归属 ROI 预估模型团队。
- DA 不再作为该问题主 owner 解释。

### DA-12 organic 是否影响结论

- 已有统一口径；不再重复入清单。
- 继续按既有口径执行。

### DA-13 campaign_name 与 campaign_id

- 默认以 `Campaign Name` 为主映射字段。
- MI 查询默认沿用 `campaign_name`。

### DA-20 国家代码 UK / GB

- `UK` / `GB` 已确认口径；不再列为 DA 待问项。

### DA-23 MI ROI 预测数组下标与模型口径

- 已确认 `ROI_N = roi_(N-1)`（0 基）：
  - `ROI7 = roi_6`
  - `ROI360 = roi_359`
- `forecast_version` 的 `_纠偏` 后缀对应 `correction`，`xy_` 前缀对应批量模型。
- MI 不直接读取 CK 离线预测表。
- 残留项：CK 的 `market_roi_prediction.roi_list` 下标差异归离线 owner，属于 `needs_decision`。
- 适用边界：`ai_ck/agent_knowledge/metrics/ROI预估实现说明.md` 的“确认口径速查”。

### DA-24a KOL 四表分工

- 已由 `nexus kol_video_effect_chart` 路径确认：
  - `kol_ad_data_list`（视频基础）
  - `kol_ad_cost_list`（投放成本与付费）
  - `kol_video_metrics_di`（视频自然互动）
  - `kolsys_resources`（达人名与分类）
- 连接键：`video_id`
- 保留待 DA 的残留问题：
  - `ai_ck profile` 的 `kol_ad_cost_list_ua` / `kol_video_metrics_v2_di` 与 MI 使用 `kol_ad_cost_list` / `kol_video_metrics_di` 是否同表。

