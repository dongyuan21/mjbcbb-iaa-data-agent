# Tools 建设 TODO

> 更新：2026-08-02
> 原则：只读；凭据不入仓；不停投/放量。

## 仍开

| 项 | 状态 | 权威入口 |
|---|---|---|
| Meta / AppLovin change log 表齐套 → ops 指标生产化 | `blocked` | `TODO/平台操作记录与素材能力待办.md` |
| 过期硬门 / 共享环境定时 / 告警通道 / MC 定时 | `blocked` / `open` | `TODO/freshness刷新自动化计划.md` |
| 工具调用审计写库（当前仅 MD/JSON 模板） | `open`（可选） | `knowledge/agent_knowledge/policies/工具调用审计输出模板.md` |

## 分析前主动提问（仍适用，非过时）

Agent 在跑分析前补**业务参数和缺失标识**；不要把「用 MI / SQL / 广告平台 API」交给用户选——那是路由责任。

优先序：verified SQL / 语义模型 → MI/CK 页面口径 → MC 事实源 →（归因后）平台操作记录工具 → 缺参再追问。

通用必问：产品/包体/store/OS；国家；异常与 baseline 窗口；all / paid_only；粒度（media / campaign / adset / ad / material / event）。

| 问题类型 | 额外要问 |
|---|---|
| DNU 下滑 | 缺管控上下文时只问 `customer_id` / `campaign_id` / account 映射或权限 |
| ROI / 回收 | sdk vs af；成熟窗口 D7/D30/D60 还是 ROI360 |
| 放量 / 停投观察 | 是否已有阈值表；只出观察名单还是动作候选 |
| 素材冷启动 | 最小曝光/消耗门槛；是否 ROI join |
| 点位复盘 | 事件池、cohort、观察窗口、扫描范围/成本上限 |

已完成的调度/告警、MI 素材探测、变更日志联查、ops SQL 模板等不再保留在
本清单；现状以 `tools/runbooks/`、相关政策和上表权威入口为准。
