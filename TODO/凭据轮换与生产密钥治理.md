# 凭据轮换与生产密钥治理

- 状态：`needs_owner_confirmation / previously_deferred`
- 优先级：`P0 before formal release`
- 更新：2026-08-02
- 来源：旧总待办曾记录 `LLM_GATEWAY_API_KEY` 可能暴露且用户选择延期；当前是否仍有效必须由 Secret owner 无回显核验，不能仅凭历史文档断言现有密钥仍泄露。

## 待办

- [ ] Secret owner 确认历史 key 是否仍有效、是否已轮换/撤销，以及受影响环境范围。
- [ ] 若仍有效，按现有 Secret 管理流程轮换或撤销；不在仓库、命令输出、trace 或对话中回显旧值/新值。
- [ ] 轮换后仅做无回显的 gateway 连通性、API/Worker/Freshness 配置一致性和最小 canary。
- [ ] 检查仓库历史、部署变量和日志安全投影是否仍存在明文；只记录扫描结论和位置，不复制 Secret。

## 完成定义

- owner 给出当前状态；需要轮换时已完成撤销/替换并验证三服务一致。
- 仓库、trace、任务日志和报告均不包含可用凭据。
- 历史事项若已在外部完成，补一条不含 Secret 的验证证据后关闭，不重复轮换。
