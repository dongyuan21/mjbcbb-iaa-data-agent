# 素材生产总表待确认项

目标表：`hungry_studio.ads_market_material_production_da`

## 待 owner / DA 确认

1. `out_flag_material` 的“任一包体×渠道 out_flag=1”来自哪张明细表或哪段生产 SQL；是否可作为素材出坡默认口径。
2. `boom_flag_material` 与 `sabcd_level_latest` 的等级来源、更新时间和优先级；S/A 是否就是当前素材爆款判定。
3. `first_bundle_id`、`first_media_source` 在同一天多个首次消耗对象并列时如何取值；是否稳定代表“首次投放路径”。
4. `product` 为空的素材如何解释；是否应归为未归属产品、历史素材、外部素材或数据缺口。
5. `designer_lib`、`designer_asset`、`designer` 是否允许在 Agent 回答中直接展示明细；若包含内部人员姓名，默认建议只做聚合或在用户明确点名时小范围展示。
6. `header_source_*`、`body_source_*`、`footer_source_*` 的来源体系是否可公开用于分析；尤其是 advertiser id/name 是否只作为来源归因维度，不作为用户级标识。
7. `test_cost_t0_t2_usd` 与 `cost_3d_total_usd` 的窗口差异是否固定为“上传后 T~T+2”与“首次投放日起 3 日”；是否存在时区或补数规则。

## 当前处理

- 表卡先标 `documentation_status: complete`，不标 `verified`。
- join 只沉淀为候选关系；跨表素材效果、ROI、消耗收入归因仍需小窗口 smoke 或 verified SQL。
- 业务动作只输出预警、筛选和候选排序，不输出自动停投、放量或预算动作。
