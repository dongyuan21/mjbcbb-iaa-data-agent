# 素材三表 MC 待确认项

来源：数仓权威钉钉《素材三表说明文档》+ 2026-07-09 live probe。

## 目标表

- `hungry_studio.ads_market_material_production_da`
- `hungry_studio.ads_market_material_media_test_da`
- `hungry_studio.ads_market_material_spend_effect_di`

## 待 owner / DA 确认

1. 三张 MC 表的正式 SLA、补数行为，以及 CK 下游 `TRUNCATE + INSERT` / `DROP PARTITION + INSERT` 的对账机制。
2. `roi0_threshold_pct` 当前硬编码阈值已由数仓权威钉钉文档确认；仍待补后续版本变更机制。
3. `prod_key` 的“4.9 公式”本体、解释口径和是否可默认展示。
4. `ads_market_material_spend_effect_di` 中活跃、游戏、广告变现、广告触发类 day0/1/2/6 字段的上游来源与成熟度；钉钉文档中这些字段仍标 TODO。
5. `path`、`file_name`、`designer*`、`user_name` 等字段是否允许明细展示；默认只做聚合、筛选或内部排查。
6. `campaign_name`、`adset_name`、`ad_name` 与投放主链路事实表 join 的稳定 key 优先级；正式分析需小窗口 smoke。

## 当前处理

- MC 表卡标 `documentation_status: complete`，表示字段/查询护栏完整，不标 `verified`。
- 可复用业务 SQL 仍需按 `da_assets/SQL晋升治理.md` 晋升后才能进入 verified SQL。
- 生产漏斗总数优先查 `ads_market_material_production_da`；渠道拆解查 `ads_market_material_media_test_da`；投放日/ad 粒度效果查 `ads_market_material_spend_effect_di`。
