# TODO · ROI360 月度人员环比工具

> 更新：2026-07-25  
> 合并自：`ROI360月度人员环比工具市场执行计划.md` + `对话内调用ROI月度人员环比工具.md`  
> 状态：`web_main_path_done / ck_reconcile_dingtalk_chat_pending`  
> 边界：只读汇总与产物；不自动停投/放量/改预算。模型不参与数值计算。

## 产品形态

```text
主页「智能分析」⟷「ROI 月度人员环比」独立规则页
  → 结构化参数 → durable job (provider=roi360_monthly_recon)
  → 规则 worker → 6 Sheet Excel + 校验摘要
```

对话内唤起（未做）必须复用同一 `POST /api/tools/roi360_monthly_person_recon/jobs`，禁止 prompt 改写锚定规则或自算。

## 已完成（Web 主链路，2026-07-16）

- [x] 主页双入口：智能分析 + ROI 月度人员环比
- [x] 工具注册、相邻月份、UA-only 默认、人员范围
- [x] 冻结导出持久化、job 入队、worker 确定性执行
- [x] 6 Sheet Excel 生成/校验、按权限下载
- [x] 最近任务列表；关页后任务可续跑

入口：`tool_registry.py`、`roi360_monthly_recon.py`、`RoiMonthlyTool.tsx`、`skills/roi360-monthly-person-recon/`。

## 仍开

### A. 对账与通知

- [ ] CK 底表重算及与冻结快照对账（输出须区分冻结主口径 vs 当前底表）
- [ ] 钉钉完成通知（失败写 event，不改主状态；不落 webhook）

### B. 对话内调用

用户在智能分析中说例如：`帮我对比 2026-05 和 2026-06 的人员 ROI 环比，按 UA-only 口径。`

- [ ] 意图路由：仅识别「相邻月份人员 ROI 环比」
- [ ] 缺月份 / 媒体范围 / 冻结导出时逐项补齐，不猜参
- [ ] 确认卡片 → 用户确认后再建 job
- [ ] 复用现有 tools jobs API；消息内展示状态、Excel 下载、校验摘要
- [ ] 刷新/换会话可恢复同一 tool job；记录 `tool_id`、参数快照、`job_id`、`artifact_id`
- [ ] 失败只回错误码与修复建议，半成品不得标成功

验收：对话与独立工具页同一规则服务与产物格式。

## 口径硬约束（勿回退）

- UA 人员月报默认 `media_scope=ua_only`（排除预装）；全渠道差异常来自预装混入
- 当月同键多人：取当月折后消耗最大行归属；上月独有键单独汇总并参与勾稽
- 产物下载走受控接口，不暴露本机绝对路径

## 待拍板（仍 `needs_decision`）

| 问题 | 建议默认 | 谁 |
|---|---|---|
| 主口径冻结快照 vs CK 当前底表 | 冻结为主，CK 对账 | 产品 / DA |
| 钉钉通知对象 | 发起人优先 | 产品 |
| 产物保留期 | 30 天 | 工程 / 合规 |

## 非目标

不做投放写操作；不自动写入知识库；不自动认定成本差异业务根因；不做成应用商店。
