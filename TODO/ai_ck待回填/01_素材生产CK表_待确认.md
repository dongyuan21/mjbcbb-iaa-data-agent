# 素材生产 CK 表待确认项

目标表：`shucang_market.material_production`

## 待 owner / DA 确认

1. CK 表与 MC `hungry_studio.ads_market_material_production_da` 的同步频率、写入方式与 SLA；当前 CK 无 `dt` 分区，默认按 `update_at` 判断新鲜度。
2. 2026-07-09 追加 live probe 发现 Distributed 查询每个 `material` 正好返回 2 行（`count()=468176`，`uniqExact(material)=234088`）；需确认是分片/副本配置、同步重复还是预期双副本读取。在确认前 Agent 生产/测试/可观测/出坡/爆款计数必须按 material 去重。
3. `observable_flag` 的完整生产公式；当前注释为“首次投放日起 >=3天”。
4. `out_flag_material` 的出坡口径和阈值当前已由数仓权威钉钉文档确认；仍待补后续阈值版本变更机制。
5. `boom_flag_material` 与 `sabcd_level_latest` 当前按数仓权威钉钉文档解释为 S/A 爆款；SABCD 上游明细公式和更新周期继续以 SABCD 表卡/生产 SQL 为准。
6. `path`、`file_name`、`designer*` 和三段来源字段是否允许在 Agent 回答中明细展示；默认只做聚合、筛选或内部排查。
7. `first_bundle_id`、`first_media_source` 在同一素材多个首次 spend 对象并列时的取值规则。
8. `product` 为空的素材归因：历史素材、外部素材、未归属产品，还是数据缺口。

## 当前处理

- 表卡标 `documentation_status: complete`，作为 L2 受限查询表使用，不标 L3 verified。
- 不直接把 CK 表当训练事实源；争议复核仍回到 MC 或 owner 确认。
- 投放治理回答只输出预警、候选排序和 `needs_decision`，不输出自动投放动作。
