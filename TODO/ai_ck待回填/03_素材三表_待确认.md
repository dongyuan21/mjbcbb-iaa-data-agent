# 素材三表待确认项

来源：数仓权威钉钉《素材三表说明文档》+ 2026-07-09 live probe。

## 目标表

- `shucang_market.material_production`
- `shucang_market.material_media_test`
- `shucang_market.material_spend_effect`

## 待 owner / DA 确认

1. CK 全量快照表 `material_production`、`material_media_test` 的正式 SLA、补数行为和与 MC `dt` 分区的对账机制。
2. `roi0_threshold_pct` 当前硬编码阈值已由数仓权威钉钉文档确认；仍待补后续版本变更机制和是否迁入配置表。
3. `prod_key` 注释为“按 4.9 公式”，公式本体、解释口径和是否可默认展示仍待补充。
4. `material_spend_effect` 中活跃、游戏、广告展示/请求/ready/触发、广告收入类 day0/1/2/6 字段的上游来源与成熟度；钉钉文档中这些字段标注为 TODO，CK schema 当前已暴露字段。
5. `material_spend_effect` 的正式延迟：2026-07-09 live probe 最新 `spend_date=2026-07-07`，暂按 T-2/T+72h 保护，不把当前日空结果当作 0。
6. `path`、`file_name`、`designer*`、`user_name` 是否允许在 Agent 回答中明细展示；默认只做聚合、筛选或内部排查。
7. `campaign_name`、`adset_name`、`ad_name` 与 ROI/spend 主链路 join 的稳定 key 优先级；当前只沉淀候选 join，正式分析需小窗口 smoke。

## 当前处理

- 三张 CK 表卡先标 `documentation_status: complete`，作为 L2 受限查询表使用，不标 L3 verified。
- 生产漏斗总数优先用 `material_production`；渠道拆解用 `material_media_test`；投放日/ad 粒度效果用 `material_spend_effect`。
- 投放治理回答只输出预警、候选排序和 `needs_decision`，不输出自动投放动作。
